# Saw-it 

**saw-it** is a lightweight CLI tool to display a folder
structure in a tree format

## Examples:

```bash
saw-it src
saw-it src > tree.txt
saw-it src > folder/tree.txt
```

```bash
structure in folder src

src/
├── folder_test
│   └── user.ts
└── index.ts
```